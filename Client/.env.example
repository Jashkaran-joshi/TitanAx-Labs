# API Base URL
# Leave empty to auto-detect (uses localhost:5000 in development, production URL from config in production)
# Only set this if you need to override the default behavior
VITE_API_BASE_URL=

# ⚠️ IMPORTANT SECURITY NOTE:
# DO NOT put any API keys, secrets, or credentials in this file!
# All sensitive API calls must be made from the backend server.
# Client-side environment variables are exposed in the browser and are NOT secure.
